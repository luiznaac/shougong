# Base URL of the shougong backend API.
# In dev, leave as "/api" and Vite proxies to VITE_API_TARGET (see vite.config.ts).
# In prod behind the reverse proxy, set to e.g. "/shougong-api" or the absolute origin.
VITE_API_BASE=/api

# Dev-only: where the Vite proxy forwards /api to.
VITE_API_TARGET=http://localhost:8080
